# ── Playify Deploy Configuration ──────────────────────────────────────────────
# Copy this file to deploy.env and fill in your values.
# NEVER commit deploy.env to git — it contains secrets.

# Supabase
SUPABASE_URL=https://fffqjbrethogesgghjsn.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# Server SSH (deploy user must have passwordless sudo for www-data operations)
DEPLOY_SERVER=deploy@104.152.50.173
WEB_PATH=/var/www/playify/
APK_DEST=/var/www/playify/download/Playify.apk
DOMAIN=playifysport.fun
